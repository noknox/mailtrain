apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mailtrain.fullname" . }}-test-connection"
  labels:
{{ include "mailtrain.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mailtrain.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
